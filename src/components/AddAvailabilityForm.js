

import React from 'react';

function AddAvailabilityForm({ onBack }) {
  return (
    <div>
      <h2>Add / Edit Availability Slot</h2>
      {/* Replace this with actual form elements */}
      <p>Form goes here...</p>

      <button onClick={onBack}>← Back to Dashboard</button>
    </div>
  );
}

export default AddAvailabilityForm;